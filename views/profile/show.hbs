<div>
    <h1>MY PROFILE</h1>
</div>


<div class="flex">
    <div class="imgBorder">
        <img class="profilePic" src="{{profile.profilePic}}" alt="profile picture">

    </div>
    <div>
        <h2>{{profile.username}}</h2>
        <h3>{{profile.slogan}}</h3>
        <button class="pure-form pure-form-aligned"><a href="{{profile._id}}/edit">Edit Profile</a></button>
        <form action="/{{profile._id}}?_method=DELETE" method="POST" class="pure-form pure-form-aligned">
            <button class="btn" type="submit">Delete Profile</button>
        </form>

    </div>
</div>



<div class="flex">
    {{#each profile.characters}}
    <div class="flex-column charSpacing">
        <h3>{{name}}</h3>
        <img class="characterImg" src="{{characterImg}}">
        <button><a href="{{../profile._id}}/character/{{_id}}" alt="view character">View Character</a></button>
    </div>
    {{/each}}
</div>